# next-genius

<!-- DK-STATE:BEGIN  generated by dkp -- do not hand-edit inside -->
## Project state (auto)

Regenerated by `dkp`. Every line below was read from disk, not remembered.

- **BUILD STANDARD**: this project follows DK's Free-Stack Playbook ($0/month, only recurring cost is the domain). Load the **`freestack`** skill BEFORE adding or choosing any service, dependency, host, database, auth, email, payment, push, storage, cron, AI provider or deploy path. Never add a paid monthly service without stating the cost and the rejected free move to DK first. Audit this project: `bash ~/.claude/skills/freestack/scripts/audit.sh "/Volumes/T7 Shield/DK-Mac/projects/next-genius"`
- **ACCOUNTS**: which Google / Cloudflare / GitHub / Firebase account owns this project, which browser profile it is logged into, and where every secret lives is in `ACCOUNTS.md` in this folder. Load the **`accounts`** skill before deploying, pushing, logging in or touching DNS. NEVER guess an account.
- **Path**: `/Volumes/T7 Shield/DK-Mac/projects/next-genius`
- **What**: Next Genius scholarship platform. Student site, admin, Apps Script automations. START HERE for any Next Genius work.
- **Last source change**: 2026-07-22 (`ACCOUNTS.md`)
- **Git**: branch `main`, 84 commits, **uncommitted changes present**
- **Last commit**: 2026-07-21  smtp-drain: retry transient Apps Script errors, shrink the claim window
- **Remote**: https://github.com/DKNMSQUAD/next-genius.git
- **Stack**: static HTML
- **URLs**, with where each one came from:
    - https://www.next-genius.com  _(curated)_
    - Only `curated` is DK-confirmed. The rest are inferred from files and may be wrong, stale, or a dead placeholder. A route returning HTTP 200 is NOT proof it is the live flow. Check the code behind a route before giving DK the link.
- **Status**: live on Cloudflare, apex 301 to www
- **Memory**: [[project_next_genius]], [[project_next_genius_domain]], [[project_next_genius_automations]], [[project_next_genius_interview]]
- **Read next**: `HANDOFF.md`

**Recent commits**

- smtp-drain: retry transient Apps Script errors, shrink the claim window
- SMTP lane: drain the mailqueue via the Workspace mailbox, not Apps Script
- Partner funnel: capture interest on click (form no longer the gate), Clicked/Interested states, nudge
- Sending watchdog: hourly CF tick (self-heal, dual-lane drain, wave-2 auto-fire, stall alert) + admin health strip
- Admin: interview blast + bulk reminder target Grade 12 students only
- Admin AO chips: combined send quota (Gmail + Brevo)

Top level: `ACCOUNTS.md`, `CLAUDE.md`, `HANDOFF.md`, `MIGRATION_2026-06-20.md`, `MIGRATION_CF_RUNBOOK.md`, `NG_Logo.avif`, `admin.html`, `apple-touch-icon.png`, `apply.html`, `assessment.html`, `browser-env.sh`, `dashboard.html`, `emails`, `favicon.png`

Index of every DK project: `/Volumes/T7 Shield/DK-Mac/projects/PROJECTS.md`. Rebuild this block with `dkp`. Write durable notes OUTSIDE these markers; anything inside is overwritten.
<!-- DK-STATE:END -->

## Notes

Anything written below or above the DK-STATE block is yours and `dkp` will never touch it. Put decisions, gotchas, and todo here.

## India Desk (added 2026-07-29)

Branding of "India Desk" by Next Genius. Front end = Denver D'Souza (Syracuse),
back office = NM/Ravisha inside the NM Squad portal.

- Public brand page: `indiadesk/index.html` -> https://www.next-genius.com/indiadesk
- Denver's desk: `indiadesk/syracuse.html` -> /indiadesk/syracuse (noindex, login-gated).
  Five modules: clock visits, clock non-visit interactions, raise flight/hotel/budget
  requests, upload accounts (bills), track applications.
- API: `functions/api/indiadesk.js`. Two callers, one endpoint - Denver with a Firebase
  ID token (next-genius-auto, whitelist in the `INDIADESK_EMAILS` secret), and the
  portal with the shared `INDIADESK_MAINT_TOKEN`. Only the maint caller can move a
  request or a bill's status.
- Store: **Firestore on next-genius-auto**, collections `indiadesk_visits`,
  `_interactions`, `_requests`, `_accounts`, `_applications`, reached with the Admin
  service account (Pages secret `FIREBASE_SA`) over the REST API. The SA bypasses
  firestore.rules, so the catch-all deny keeps these collections off every client.
  No listeners. D1 was the first choice and `schema/indiadesk.sql` still maps 1:1,
  but creating a D1 database needs a work-account token with D1:Edit and the only
  token on this Mac is Pages-scope. `uni` field = the desk slug, so a second
  university is a field value, not a new collection.
- Bills go to Cloudinary (`dclcl4mox`, unsigned preset `otg_unsigned`, folder
  `india-desk`) relayed FROM Cloudflare, never from the counsellor's laptop -
  api.cloudinary.com is unreachable on some Indian networks.
- Free stack: CF Pages + Pages Functions + D1 + existing Firebase auth + Cloudinary.
  $0/month, no new service.
- LIVE since 2026-07-29. Provisioning runbook: `scripts/indiadesk-setup.sh`.
  Secrets on next-genius: `FIREBASE_SA`, `INDIADESK_MAINT_TOKEN`, `INDIADESK_EMAILS`
  (still empty - Denver's Google account has not been given yet, so only DK, Neeraj
  and helpdesk@ can open the desk). Same `INDIADESK_MAINT_TOKEN` on nm-squad-crm.
- Portal side lives in nm-squad-portal: `functions/api/indiadesk.js`,
  `src/lib/indiadesk.ts`, `src/team/indiadesk/`.
